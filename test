#!/bin/zsh
cd "/Users/lichengbin/Desktop/1. Work Files/haijixing_dashboard"

# 激活虚拟环境
source .venv/bin/activate

# 启动（用 python -m 最稳）
python3 -m streamlit run app.py

# 让窗口不自动关（可选）
echo ""
echo "已启动。按回车关闭此窗口…"
read